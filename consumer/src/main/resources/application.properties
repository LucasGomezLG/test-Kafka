# Message broker config
spring.cloud.stream.binders.local.type=kafka
spring.cloud.stream.binders.local.environment.spring.kafka.host=${KAFKA_HOST:localhost}
# spring.cloud.stream.binders.local.environment.spring.rabbitmq.username=${RABBIT_USERNAME:guest}
# spring.cloud.stream.binders.local.environment.spring.rabbitmq.password=${RABBIT_PWD:guest}
spring.cloud.stream.bindings.pending_tasks.destination=test
